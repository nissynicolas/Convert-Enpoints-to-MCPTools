# SimpleApiMcpServer

A Model Context Protocol (MCP) server generated from OpenAPI specification.

## 🚀 Quick Start

### Prerequisites
- **.NET 9.0 SDK** or later
- **VS Code** with MCP extensions or **Cursor IDE** (recommended)

### Build and Run

```bash
# Build the project
dotnet build

# Run the MCP server
dotnet run
```

The server communicates via JSON-RPC over stdin/stdout, making it compatible with any MCP client.

## 🛠️ Integration with IDEs

### Cursor IDE (Recommended)
Add to your Cursor MCP configuration:

```json
{
  "mcpServers": {
    "simpleapimcpserver": {
      "command": "dotnet",
      "args": ["run", "--project", "path/to/SimpleApiMcpServer", "--no-build"],
      "cwd": "path/to/SimpleApiMcpServer"
    }
  }
}
```

## 📚 Available Tools

This MCP server provides **3 tools** generated from the OpenAPI specification:

- **GetUsers**: Get all users
- **CreateUser**: Create a new user
- **GetUserById**: Get user by ID

## 📋 Usage Examples

### List All Available Tools
```json
{"jsonrpc": "2.0", "id": 1, "method": "tools/list"}
```

### Example Tool Call
```json
{
  "jsonrpc": "2.0", "id": 2, "method": "tools/call",
  "params": {
    "name": "GetUsers",
    "arguments": {}
  }
}
```

## ⚙️ Configuration

**Base URL**: This server is configured to use `https://api.example.com/v1` as the API base URL.

## 🔧 Technical Details

- **Framework**: .NET 9.0
- **MCP SDK**: ModelContextProtocol v0.4.0-preview.1
- **Transport**: JSON-RPC over stdin/stdout
- **Generated Tools**: 3

## 📄 License

This generated project follows the same license as the source OpenAPI specification.

---

**Generated by OpenAPI to MCP Server Generator**